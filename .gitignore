# Local credentials — generated at build time from Netlify env vars
config.js

# FTP credentials — never commit
.vscode/sftp.json

# macOS
.DS_Store

# Browser-saved page artifacts
*_files/

# Dependencies
node_modules/
