# Local credentials — generated at deploy time, never commit
config.js

# FTP credentials — never commit
.vscode/sftp.json

# macOS
.DS_Store

# Browser-saved page artifacts
*_files/

# Dependencies
node_modules/
