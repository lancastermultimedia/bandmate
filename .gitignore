# config.js committed on redesign/montague-nasa for Netlify — see branch notes

# FTP credentials — never commit
.vscode/sftp.json

# macOS
.DS_Store

# Browser-saved page artifacts
*_files/

# Dependencies
node_modules/
